import React, { Component } from "react";

const handleSort = () => {
  const [title_asc, title_dcs, due_asc, due_dcs] = useState("");

  if (useState(title_asc)) {
    console.log("Clicked");
  }
};

export default handleSort;
